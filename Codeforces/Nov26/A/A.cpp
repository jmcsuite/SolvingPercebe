#include<bits/stdc++.h>
using namespace std;
using ll = long long;
using vll = vector<long long>;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    ll T; cin>>T;
    while(T--){
        ll N; cin>>N;
        ll a;
        for(int i = 0; i  < N; i++) cin >> a;
        cout << 1 << ' ' << N <<  '\n';
    }
}